# Keymaker environment example
# Copy this file to .env.local for local development
# NEVER commit real secrets in .env.local

###############################################
# App & Host
###############################################
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3001

# Dev server port is configured in package.json (next dev -p 3001)

###############################################
# Solana RPC / WS (Helius or public RPC)
###############################################
# Public (exposed to browser) read endpoint
NEXT_PUBLIC_HELIUS_RPC=https://api.mainnet-beta.solana.com

# Server-side RPC (can be the same as above)
HELIUS_RPC_URL=https://api.mainnet-beta.solana.com

# Optional WS endpoint for heartbeat status (if available)
# HELIUS_WS_URL=wss://your-ws-endpoint

###############################################
# Security Secrets (REQUIRED in production)
###############################################
# Used to encrypt keystore files at rest (>= 12 chars)
KEYMAKER_MASTER_PASSPHRASE=change-this-to-a-long-unique-passphrase

# Used to sign/verify sessions (>= 32 chars)
KEYMAKER_SESSION_SECRET=change-this-to-a-very-long-random-secret-value

# Protects engine/market API endpoints (>= 32 chars)
ENGINE_API_TOKEN=change-this-to-a-long-random-token

###############################################
# Safety Rails (Flip sequence: DRY -> ARMED -> LIVE)
###############################################
DRY_RUN_DEFAULT=YES
KEYMAKER_REQUIRE_ARMING=YES
KEYMAKER_ALLOW_LIVE=NO

# Emergency live kill switch for engine routes (YES/NO)
KEYMAKER_DISABLE_LIVE_NOW=NO

###############################################
# Rate Limiting (Optional: Upstash Redis)
###############################################
# UPSTASH_REDIS_REST_URL=https://<id>.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxx

###############################################
# Jito (Optional)
###############################################
# JITO tip floor API for Status Bento
JITO_TIP_API=https://mainnet.block-engine.jito.wtf/api/v1/tip_floor

###############################################
# Build/Runtime (Optional)
###############################################
# For standalone Next.js output (used by build:standalone)
# NEXT_STANDALONE=1

###############################################
# Observability (Optional)
###############################################
# SENTRY_DSN=

###############################################
# Tooling / Smoke (Local)
###############################################
SMOKE_BASE_URL=http://localhost:3001
