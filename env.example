###############################################
# Keymaker v1.3.0 — Environment Configuration #
###############################################

# --- Core APIs ---
HELIUS_API_KEY=
BIRDEYE_API_KEY=
JUPITER_API_KEY=
TWO_CAPTCHA_KEY=
PUMP_FUN_API_KEY=

# Optional Jito auth (paid tier)
JITO_AUTH_TOKEN=
JITO_WS_URL=

# Jito free-tier RPC (keeps tip ≤ 50,000 lamports via Zod rule)
JITO_RPC_URL=https://mainnet.block-engine.jito.wtf/

# --- App Network ---
# Accepted values: dev-net | main-net (UI). Internally transforms to devnet | mainnet-beta
NETWORK=dev-net

# --- Fees & Tips ---
# Max 50,000 lamports enforced when JITO_RPC_URL is free-tier
JITO_TIP_LAMPORTS=5000
# Jupiter swap fee in basis points (0-100)
JUPITER_FEE_BPS=5

# --- Headless & Tests ---
# Puppeteer headless timeout (seconds)
HEADLESS_TIMEOUT=30
# Deterministic seed for test wallets
DETERMINISTIC_SEED=episode-kingdom-sunshine-alpha

# --- Optional overrides ---
# If set, used by health checks and UI defaults
NEXT_PUBLIC_HELIUS_RPC=https://api.devnet.solana.com
