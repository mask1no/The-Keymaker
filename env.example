#############################################
# The Keymaker – Environment Example
#############################################

# Web (public)
NEXT_PUBLIC_WS_URL=ws://localhost:8787
NEXT_PUBLIC_DEFAULT_RPC=https://api.mainnet-beta.solana.com

# Daemon (server only)
RPC_URL=https://api.mainnet-beta.solana.com
JITO_REGION=NY
DB_FILE=./apps/daemon/keymaker.sqlite
KEYSTORE_FILE=./apps/daemon/keystore.json
# Temporary: keystore password for local-only encryption
KEYSTORE_PASSWORD=change-me
# Optional dev-only: master wallet secret to test funding without desktop signer
MASTER_SECRET_BASE58=

