# ==========================================
# Keymaker Environment Configuration
# ==========================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ==========================================
# CORE CONFIGURATION (Required)
# ==========================================

# Path to payer keypair JSON (required for funding, sweeping, bundle signing)
# Generate with: solana-keygen new -o ~/keymaker-payer.json -s
KEYPAIR_JSON=/path/to/your/keymaker-payer.json

# Session secret for HMAC signing (required in production)
# Generate with: openssl rand -hex 32
KEYMAKER_SESSION_SECRET=your-64-char-hex-secret-here

# ==========================================
# RPC ENDPOINTS (Required)
# ==========================================

# Primary RPC URL (Helius recommended for production)
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# Public RPC URL (exposed to client for read operations)
NEXT_PUBLIC_HELIUS_RPC=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# ==========================================
# NETWORK CONFIGURATION
# ==========================================

# Network selection: mainnet-beta | devnet
NEXT_PUBLIC_NETWORK=mainnet-beta

# ==========================================
# API SECURITY (Optional but Recommended)
# ==========================================

# Engine API token (protects /api/engine/* endpoints)
# Generate with: openssl rand -hex 16
ENGINE_API_TOKEN=your-random-token-here

# ==========================================
# EXTERNAL API KEYS (Optional)
# ==========================================

# Birdeye API key (for token price data and market info)
# Get from: https://birdeye.so
BIRDEYE_API_KEY=your-birdeye-api-key

# CoinGecko API key (for BTC/ETH/SOL/CAKE prices)
# Get from: https://www.coingecko.com/en/api
COINGECKO_API_KEY=your-coingecko-api-key

# ==========================================
# JITO CONFIGURATION
# ==========================================

# Default Jito tip in lamports (5000 = 0.000005 SOL)
NEXT_PUBLIC_JITO_TIP_LAMPORTS=5000

# Jupiter swap fee in basis points (5 = 0.05%)
NEXT_PUBLIC_JUPITER_FEE_BPS=5

# ==========================================
# EXECUTION DEFAULTS
# ==========================================

# Default priority fee level: low | med | high | vhigh
PRIORITY=med

# Default tip amount in lamports
TIP_LAMPORTS=5000

# Recent blockhash override (leave empty for automatic fetching)
BLOCKHASH=

# ==========================================
# MONITORING & OBSERVABILITY (Optional)
# ==========================================

# Sentry DSN for error tracking
# Get from: https://sentry.io
SENTRY_DSN=your-sentry-dsn

# Sentry auth token for releases
SENTRY_AUTH_TOKEN=your-sentry-token

# Sentry organization and project
SENTRY_ORG=your-org
SENTRY_PROJECT=your-project

# ==========================================
# DEVELOPMENT OPTIONS
# ==========================================

# Enable Next.js standalone build (for Docker/production)
NEXT_STANDALONE=0

# Enable bundle analyzer
ANALYZE=false

# Database path override (defaults to ./data/keymaker.db)
# DB_PATH=/custom/path/to/keymaker.db

# ==========================================
# RATE LIMITING (Optional, uses defaults)
# ==========================================

# Payload size limits (bytes)
# PAYLOAD_LIMIT_TRADES_BYTES=32768
# PAYLOAD_LIMIT_ENGINE_BYTES=524288

# Max array sizes
# TRADES_MAX_TX_IDS=50
# TRADES_MAX_WALLETS=50

# ==========================================
# NOTES
# ==========================================
# 1. KEYPAIR_JSON and KEYMAKER_SESSION_SECRET are REQUIRED
# 2. At least one RPC URL is required (Helius recommended)
# 3. ENGINE_API_TOKEN strongly recommended for production
# 4. Birdeye/CoinGecko keys optional but improve data quality
# 5. Never commit .env file to version control
# 6. Keep KEYPAIR_JSON secure - it controls real funds

