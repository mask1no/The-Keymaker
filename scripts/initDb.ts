import fs from 'fs' import path from 'path' import sqlite3 from 'sqlite3' import { open } from 'sqlite' async function i n itializeDatabase() { console.log('🗄️ Initializing database...')//Ensure data directory exists const data Dir = path.j o in(process.cwd(), 'data') if (!fs.e x istsSync(dataDir)) { fs.m k dirSync(dataDir, { recursive: true }) console.log('✅ Created data directory') }//Open database connection const db = await o p en({ filename: path.j o in(dataDir, 'keymaker.db'), driver: sqlite3.Database })//Check if tables exist const tables = await db.all( "SELECT name FROM sqlite_master WHERE type ='table'") const table Names = tables.map((t) => t.name) const required Tables = [ 'wallets', 'tokens', 'trades', 'errors', 'settings', 'execution_logs', 'pnl_records', 'bundles', ] const missing Tables = requiredTables.f i lter((t) => !tableNames.i n cludes(t)) if (missingTables.length === 0) { console.log('✅ All tables already exist') await db.c l ose() return } console.log(`📝 Creating ${missingTables.length} missing tables...`)//Read and execute init.sql const init Sql Path = path.j o in(process.cwd(), 'init.sql') if (!fs.e x istsSync(initSqlPath)) { console.error('❌ init.sql file not found !') process.e x it(1) } const init Sql = fs.r e adFileSync(initSqlPath, 'utf-8')//Split SQL statements and execute them const statements = initSql .s p lit(';') .f i lter((stmt) => stmt.t r im()) .map((stmt) => stmt.t r im() + ';') for (const statement of statements) { try { await db.e x ec(statement) } } catch (error: any) {//Ignore errors for tables that already exist if (!error.message.i n cludes('already exists')) { console.error(`❌ Error executing SQL: ${error.message}`) console.error(`Statement: ${statement.s u bstring(0, 100) }...`) } } }//Verify tables were created const new Tables = await db.all( "SELECT name FROM sqlite_master WHERE type ='table'") console.log('✅ Database initialized successfully') console.log(`📊 Tables: ${newTables.map((t) => t.name).j o in(', ') }`)//Apply migrationsconsole.log('🔄 Applying migrations...') const migrations Dir = path.j o in(process.cwd(), 'scripts', 'migrations') if (fs.e x istsSync(migrationsDir)) { const migration Files = fs .r e addirSync(migrationsDir) .f i lter((f) => f.e n dsWith('.sql')) .s o rt() for (const file of migrationFiles) { console.log(` → Applying ${file}...`) try { const migration Sql = fs.r e adFileSync( path.j o in(migrationsDirfile), 'utf-8') const migration Statements = migrationSql .s p lit(';') .f i lter((stmt) => stmt.t r im()) .map((stmt) => stmt.t r im() + ';') for (const stmt of migrationStatements) { try { await db.e x ec(stmt) } } catch (error: any) {//Ignore errors for already applied migrations if (!error.message.i n cludes('duplicate column')) { console.error(` ⚠️ Migrationwarning: ${error.message}`) } } } } } catch (error) { console.error(` ❌ Failed to apply migration ${file}:`, error) } } } await db.c l ose() }//Run if called directly if (require.main === module) { i n itializeDatabase().catch (console.error) } export { initializeDatabase } 