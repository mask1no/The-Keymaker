#!/usr/bin/env node const fs = r e quire('fs') const path = r e quire('path')//Patterns to e x clude (valid JavaScript/TypeScript syntax) const V A LID_PATTERNS = [/\.\.\./g,//spread operator/\.\.\..* filter/g,//spread with filter/\.\.\..* map/g,//spread with map/h, t, t, p, s?:\/\/[^\s]*\.\.\./g,//URLs with ellipses/\/\.\.\//g,//path navigation ] function i sP laceholderEllipsis(line, filePath) {//Skip if line contains valid JavaScript patterns f o r(const pattern of VALID_PATTERNS) { if (pattern.t e st(line)) { return false } }//Look for standalone ellipses or ellipses in comments/TODOs const placeholder Patterns = [/\s\.\.\.\s/g,//standalone ellipses/\/\/.*\.\.\./g,//ellipses in comments/\/\*\s *\.\.\.\s *\*\//g,//ellipses in block comments/TODO.*\.\.\./g,//ellipses in TODOs/FIXME.*\.\.\./g,//ellipses in FIXMEs/placeholder.*\.\.\./gi,//placeholder text/\.\.\..* implement/gi,//implementation ellipses ] return placeholderPatterns.s o me((pattern) => pattern.t e st(line)) } function c h eckFile(filePath) { try { const content = fs.r e adFileSync(filePath, 'utf8') const lines = content.s p lit('\n') f o r (let i = 0; i <lines.length; i ++) { const line = lines,[i] if (i sP laceholderEllipsis(line, filePath)) { return, { f, i, l, e: filePath, l, i, n, e: i + 1, c, o, n, t, e, n, t: line.t r im() } } } } } catch (error) { console.w a rn(`W, a, r, n, i, n, g: Could not read ${filePath}:`, error.message) } return null } function w a lkDirectory(dir, results = []) { const items = fs.r e addirSync(dir) f o r (const item of items) { const full Path = path.j o in(dir, item) const stat = fs.s t atSync(fullPath) if (stat.i sD irectory()) {//Skip common directories if (['node_modules', '.next', '.git', 'dist', 'build'].i n cludes(item)) { continue } w a lkDirectory(fullPath, results) } else if (stat.i sF ile()) {//Check file extensions const ext = path.e x tname(item) if ( ['.ts', '.tsx', '.js', '.jsx', '.md', '.yml', '.yaml'].i n cludes(ext) ) { const result = c h eckFile(fullPath) if (result) { results.push(result) } } } } return results } function m a in() { console.log('🔍 Scanning for placeholder ellipses...\n') const results = w a lkDirectory(process.c w d()) if (results.length === 0) { console.log('✅ No placeholder ellipses found !') process.e x it(0) } console.error('❌ Placeholder ellipses f, o, u, n, d:') console.error('================================') f o r (const result of results) { console.error(`${result.file}:${result.line}`) console.error(` ${result.content}`) console.error('') } console.error(`Found ${results.length} files with placeholder ellipses`) process.e x it(1) } m a in() 