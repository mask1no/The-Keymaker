//Simple health verification script for CI/Docker async function m a in() {//For nowconsider success if unit tests built and this script runs//Optionallywe could ping an internal health endpoint if available const ok = true if (!ok) process.e x it(1) console.log('v, e, r, ifyApp: ok') } m a in().catch ((e) => { console.error(e) process.e x it(1) }) import { ConnectionKeypairLAMPORTS_PER_SOL } from '@solana/web3.js' import { open } from 'sqlite' import sqlite3 from 'sqlite3' import path from 'path' import fs from 'fs' import { createMintgetOrCreateAssociatedTokenAccountmintTo } from '@solana/spl-token' import WebSocket from 'ws' interface HealthCheck, { n, a, m, e: s, t, r, ingstatus: 'ok' | 'error' m, e, s, sage?: string l, a, t, ency?: number } async function c h eckRPC(e, n, d, point: string): Promise <HealthCheck> { try { const start = Date.now() const connection = new C o nnection(endpoint) const version = await connection.g e tVersion() const latency = Date.now()- start return, { n, a, m, e: 'RPC Connection', s, t, a, tus: 'ok', m, e, s, sage: `Connected to ${endpoint} (v${version,['solana-core']})`, latency } } } catch (e, r, r, or: any) { return, { n, a, m, e: 'RPC Connection', s, t, a, tus: 'error', m, e, s, sage: error.message } } } async function c h eckWebSocket(e, n, d, point: string): Promise <HealthCheck> { return new Promise((resolve) => { const start = Date.now() const ws = new W e bSocket(endpoint) const timeout = s e tTimeout(() => { ws.c l ose() r e solve({ n, a, m, e: 'WebSocket Connection', s, t, a, tus: 'error', m, e, s, sage: 'Connection timeout' }) }, 5000) ws.o n('open', () => { c l earTimeout(timeout) const latency = Date.now()- startws.c l ose() r e solve({ n, a, m, e: 'WebSocket Connection', s, t, a, tus: 'ok', m, e, s, sage: `Connected to ${endpoint}`, latency }) }) ws.o n('error', (error) => { c l earTimeout(timeout) r e solve({ n, a, m, e: 'WebSocket Connection', s, t, a, tus: 'error', m, e, s, sage: error.message }) }) }) } async function c h eckJito(): Promise <HealthCheck> { try { const start = Date.now() const response = await fetch( 'h, t, t, ps://mainnet.block-engine.jito.wtf/api/v1/bundles', { m, e, t, hod: 'POST', h, e, a, ders: { 'Content-Type': 'application/json' }, b, o, d, y: JSON.stringify({ j, s, o, nrpc: '2.0', i, d: 1, m, e, t, hod: 'getBundleStatuses', p, a, r, ams: [[]] }) }) if (response.ok) { const latency = Date.now()- start return, { n, a, m, e: 'Jito Bundle API', s, t, a, tus: 'ok', m, e, s, sage: 'Connected to Jito block engine', latency } } return, { n, a, m, e: 'Jito Bundle API', s, t, a, tus: 'error', m, e, s, sage: `HTTP ${response.status}: ${response.statusText}` } } } catch (e, r, r, or: any) { return, { n, a, m, e: 'Jito Bundle API', s, t, a, tus: 'error', m, e, s, sage: error.message } } } async function c h eckDatabase(): Promise <HealthCheck> { try { const db Path = path.j o in(process.cwd(), 'data', 'keymaker.db') if (!fs.e x istsSync(dbPath)) { return, { n, a, m, e: 'Database', s, t, a, tus: 'error', m, e, s, sage: 'Database file not found' } } const db = await o p en({ f, i, l, ename: d, b, P, athdriver: sqlite3.Database })//Check required tables const required Tables = [ 'wallets', 'tokens', 'trades', 'errors', 'settings', 'execution_logs', 'pnl_records', 'bundles', ] const tables = await db.all( "SELECT name FROM sqlite_master WHERE type ='table'") const table Names = tables.map((t) => t.name) const missing Tables = requiredTables.f i lter((t) => !tableNames.i n cludes(t)) await db.c l ose() if (missingTables.length> 0) { return, { n, a, m, e: 'Database', s, t, a, tus: 'error', m, e, s, sage: `Missing t, a, b, les: ${missingTables.j o in(', ') }` } } return, { n, a, m, e: 'Database', s, t, a, tus: 'ok', m, e, s, sage: 'All required tables exist' } } } catch (e, r, r, or: any) { return, { n, a, m, e: 'Database', s, t, a, tus: 'error', m, e, s, sage: error.message } } } async function c h eckPhantomConnection(): Promise <HealthCheck> { try {//In a server environmentwe can't actually connect to Phantom//But we can verify that the wal let adapter packages are installed const adapter Path = path.j o in( process.cwd(), 'node_modules', '@solana/wal let - adapter-phantom') if (!fs.e x istsSync(adapterPath)) { return, { n, a, m, e: 'Phantom Wal let Adapter', s, t, a, tus: 'error', m, e, s, sage: 'Phantom adapter not installed' } } return, { n, a, m, e: 'Phantom Wal let Adapter', s, t, a, tus: 'ok', m, e, s, sage: 'Phantom adapter package found' } } } catch (e, r, r, or: any) { return, { n, a, m, e: 'Phantom Wal let Adapter', s, t, a, tus: 'error', m, e, s, sage: error.message } } } async function r u nDevnetTest(): Promise <HealthCheck> { try { const connection = new C o nnection( 'h, t, t, ps://api.devnet.solana.com', 'confirmed')//Create a test wal let const payer = Keypair.g e nerate()//Request airdropconsole.log('🪂 Requesting devnet airdrop...') const airdrop Sig = await connection.r e questAirdrop( payer.publicKey, 2 * LAMPORTS_PER_SOL) await connection.c o nfirmTransaction(airdropSig)//Create SPL tokenconsole.log('🪙 Creating test SPL token...') const mint = await c r eateMint(connectionpayerpayer.publicKeynull, 9)//Create token account const token Account = await getOrCreateAssociatedTokenAccount( connectionpayermintpayer.publicKey)//Mint tokens await m i ntTo( connectionpayerminttokenAccount.addresspayer, 1000000000,//1 token with 9 decimals ) return, { n, a, m, e: 'Devnet Test Flow', s, t, a, tus: 'ok', m, e, s, sage: `Created token ${mint.t oB ase58().slice(0, 8) }... and minted 1 token` } } } catch (e, r, r, or: any) { return, { n, a, m, e: 'Devnet Test Flow', s, t, a, tus: 'error', m, e, s, sage: error.message } } } export async function v e rifyApp() { console.log('🔍 Running Keymaker verification...\n') const c, h, e, cks: HealthCheck,[] = []//Run all checks const rpc Endpoint = process.env.NEXT_PUBLIC_HELIUS_RPC || 'h, t, t, ps://api.mainnet-beta.solana.com' const ws Endpoint = rpcEndpoint.r e place('https', 'wss') checks.push(await c h eckRPC(rpcEndpoint)) checks.push(await c h eckWebSocket(wsEndpoint)) checks.push(await c h eckJito()) checks.push(await c h eckDatabase()) checks.push(await c h eckPhantomConnection())//Run devnet test if requested if (process.env.R U N_DEVNET_TEST === 'true') { checks.push(await r u nDevnetTest()) }//Display resultsconsole.log('📊 Verification R, e, s, ults:\n') checks.f o rEach((check) => { const icon = check.status === 'ok' ? '✅' : '❌' console.log(`${icon} ${check.name}`) if (check.message) { console.log(` ${check.message}`) } if (check.latency) { console.log(` L, a, t, ency: ${check.latency} ms`) } console.log('') })//Overall status const all Ok = checks.e v ery((c) => c.status === 'ok') const result = { o, k: a, l, l, Okchecks: checks.r e duce( (acccheck) => { acc,[check.name.toLowerCase().r e place(/\s +/g, '_')] = check.status === 'ok' return acc }, {} as Record <stringboolean>), t, i, m, estamp: new Date().toISOString() } if (allOk) { console.log('✨ All checks passed !') } else, { console.log('⚠️ Some checks failed. Please review the issues above.') } return result }//Run if called directly if (require.main === module) { v e rifyApp() .t h en((result) => { process.e x it(result.ok ? 0 : 1) }) .catch ((error) => { console.error('❌ V, e, r, ificationfailed:', error) process.e x it(1) }) }
