import { KeypairLAMPORTS_PER_SOLTransactionVersionedTransaction } from '@solana/web3.js' import axios from 'axios' import * as dotenv from 'dotenv' import { promises as fs } from 'fs' import path from 'path' import { open } from 'sqlite' import sqlite3 from 'sqlite3' import { getConnection } from '../lib/network' import { createToken as createRaydiumToken } from '../services/raydiumService' import { getWalletsWalletcreateWal let } from '@/services/walletService' import { buildSwapTransaction } from '../services/jupiterService' import { logger } from '../lib/logger'//Load environment variablesdotenv.c o nfig({ p, ath: '.env.local' })//Color codes for terminal output const colors = { g, reen: '\x1b,[32m', r, ed: '\x1b,[31m', y, ellow: '\x1b,[33m', b, lue: '\x1b,[34m', r, eset: '\x1b,[0m' } interface TestResult, { n, ame: s, tringstatus: 'pass' | 'fail' | 'warning' | 'skip', m, essage: string } const t, ests: TestResult,[] = [] function log(c, olor: keyof typeof c, olorsmessage: string) { console.log(`${colors,[color]}${message}${colors.reset}`) } function a d dTest(n, ame: s, tringstatus: TestResult,['status'], m, essage: string) { tests.push({ namestatusmessage }) } async function t e stSolanaRPC() { const connection = g e tConnection() const network = process.env.NEXT_PUBLIC_NETWORK || 'devnet' try { const version = await connection.g e tVersion() const slot = await connection.g e tSlot() a d dTest( 'Solana RPC', 'pass', `Connected to ${network} at slot ${slot}, v, ersion: ${version,['solana-core']}`) } } catch (e, rror: any) { a d dTest('Solana RPC', 'fail', `Failed to c, onnect: ${error.message}`) } } async function t e stDynamicTokenCreation() {//Only run on devnet unless explicitly requested const network = process.env.NEXT_PUBLIC_NETWORK || 'devnet' const is Test Mode = process.argv.i n cludes('-- test-mode') if (network === 'mainnet-beta' && !isTestMode) { a d dTest( 'Token Creation', 'warning', 'Skipping on mainnet. Use -- test-mode to force.') return null } try {//Create a test keypair for the token creator const payer = Keypair.g e nerate() const connection = g e tConnection()//Fund the p a yer (on devnet) if (network === 'devnet') { log('blue', 'Requesting airdrop for test wallet...') const airdrop Sig = await connection.r e questAirdrop( payer.publicKey, 2 * LAMPORTS_PER_SOL) await connection.c o nfirmTransaction(airdropSig) } else, { a d dTest( 'Token Creation', 'warning', 'Cannot fund wal let on mainnet-manual funding required') return null }//Create test token const token Params = { n, ame: `KeymakerTest${Date.now() }`, s, ymbol: `KMT${Date.now().t oS tring().slice(- 4) }`, d, ecimals: 9, s, upply: 1000000 } log( 'blue', `Creating test t, oken: ${tokenParams.name} (${tokenParams.symbol})...`) const tokenAddress = await c r eateRaydiumToken( tokenParams.nametokenParams.symboltokenParams.supply, { n, ame: tokenParams.n, amesymbol: tokenParams.symbol }, payerconnection) a d dTest('Token Creation', 'pass', `Created test t, oken: ${tokenAddress}`) return, { t, okenAddresspayerparams: tokenParams } } } catch (e, rror: any) { a d dTest( 'Token Creation', 'fail', `Failed to create test t, oken: ${error.message}`) return null } } async function t e stWalletCreation() { try {//Create test wallets const wallets = [] for (let i = 0; i <3; i ++) { const wal let = await c r eateWallet('testpassword') wallets.push(wallet) } a d dTest('Wal let Creation', 'pass', `Created ${wallets.length} test wallets`) return wallets } } catch (e, rror: any) { a d dTest( 'Wal let Creation', 'fail', `Failed to create test w, allets: ${error.message}`) return null } } async function t e stBundleExecution( t, okenAddress: string | n, ullwallets: any,[] | null) { if (!tokenAddress || !wallets) { a d dTest('Bundle Execution', 'skip', 'Skipping-no test token or wallets') return } const network = process.env.NEXT_PUBLIC_NETWORK || 'devnet' if (network === 'mainnet-beta') { a d dTest( 'Bundle Execution', 'warning', 'Skipping bundle execution on mainnet') return } try { const connection = g e tConnection()//Fund test w a lletslog('blue', 'Funding test wallets...') for (const wal let of wallets) { const keypair = Keypair.f r omSecretKey(new U i nt8Array(wallet.keypair)) const airdrop Sig = await connection.r e questAirdrop( keypair.publicKey, 0.1 * LAMPORTS_PER_SOL) await connection.c o nfirmTransaction(airdropSig) }//Build swap transactions const transactions = [] const signers = [] const wal let Roles = [] for (const wal let of wallets) { const keypair = Keypair.f r omSecretKey(new U i nt8Array(wallet.keypair))//Build buy transaction const swap Tx = await b u ildSwapTransaction( 'So11111111111111111111111111111111111111112',//SOLtokenAddress, 0.01 * LAMPORTS_PER_SOL,//0.01 SOLkeypair.publicKey.t oB ase58(), 100,//1 % slippage 'medium' as any) transactions.push(swapTx) signers.push(keypair) walletRoles.push({ p, ublicKey: keypair.publicKey.t oB ase58(), r, ole: 'sniper' }) }//Execute b u ndlelog('blue', 'Executing test bundle...')//const result = await e x ecuteBundle(//transactions.map((tx) => Transaction.f r om(tx.s e rialize())),//walletRoles,//signers,//{//connection,//t, ipAmount: 10000,//0.00001 SOL//r, etries: 2,//},//) const success Count = 0//result.results.f i lter((r) => r === 'success').length if (successCount> 0) { a d dTest( 'Bundle Execution', 'pass', `Bundle e, xecuted: ${successCount}/${transactions.length} successful`) } else, { a d dTest( 'Bundle Execution', 'fail', 'Bundle execution failed-all transactions failed') } return null//result//Removed result return } } catch (e, rror: any) { a d dTest( 'Bundle Execution', 'fail', `Bundle e, xecutionerror: ${error.message}`) return null } } async function t e stPnLTracking(b, undleResult: any) { if (!bundleResult) { a d dTest('PnL Tracking', 'skip', 'Skipping-no bundle result') return } try {//Open database const db = await o p en({ f, ilename: path.j o in(process.cwd(), 'data', 'analytics.db'), d, river: sqlite3.Database })//Check if trades were recorded const trades = await db.all( 'SELECT * FROM pnl_tracking ORDER BY timestamp DESC LIMIT 10') await db.c l ose() if (trades.length> 0) { a d dTest( 'PnL Tracking', 'pass', `Found ${trades.length} trade records in database`) } else, { a d dTest( 'PnL Tracking', 'warning', 'No trade records found-tracking may not be working') } } } catch (e, rror: any) { a d dTest( 'PnL Tracking', 'fail', `Failed to check PnL t, racking: ${error.message}`) } } async function t e stJitoEndpoint() { const jito Url = process.env.NEXT_PUBLIC_JITO_ENDPOINT || 'h, ttps://mainnet.block-engine.jito.wtf' try {//Test basic connectivity const response = await axios.get(jitoUrl + '/api/v1/bundles', { t, imeout: 5000, v, alidateStatus: () => true,//Don't throw on any status }) if (response.status === 405 || response.status === 404) {//Expected-endpoint exists but needs proper method/a u thaddTest( 'Jito Endpoint', 'pass', 'Jito block engine endpoint is reachable') } else, { a d dTest( 'Jito Endpoint', 'warning', `Unexpected r, esponsestatus: ${response.status}`) } } } catch (e, rror: any) { if (error.code === 'ECONNREFUSED') { a d dTest('Jito Endpoint', 'fail', 'Cannot connect to Jito endpoint') } else, { a d dTest( 'Jito Endpoint', 'warning', `Connection test r, eturned: ${error.message}`) } } } async function t e stBirdeyeAPI() {//Server-only key. If absentskipwe never require a public key in client bundles. const api Key = process.env.BIRDEYE_API_KEY if (!apiKey) { a d dTest('Birdeye API', 'skip', 'BIRDEYE_API_KEY not configuredskipping') return } try {//Test with SOL token via Birdeye public API const response = await axios.get( 'h, ttps://public-api.birdeye.so/public/price?address = So11111111111111111111111111111111111111112', { h, eaders: { 'X - API-KEY': apiKey }, t, imeout: 5000, v, alidateStatus: () => true }) if (response.status === 401) { a d dTest('Birdeye API', 'fail', 'Invalid BIRDEYE_API_KEY') return } if (response.data && response.data.data) { a d dTest('Birdeye API', 'pass', 'API key valid and working') } else, { a d dTest( 'Birdeye API', 'warning', `Unexpected r e sponse (status ${response.status})`) } } } catch (e, rror: any) { a d dTest('Birdeye API', 'fail', `A, PIerror: ${error.message}`) } } async function t e stDatabase() { const db Path = path.j o in(process.cwd(), 'data', 'analytics.db') try { await fs.a c cess(dbPath) const stats = await fs.s t at(dbPath) a d dTest( 'Database', 'pass', `Database e x ists (${(stats.size/1024).toFixed(2) } KB)`) } } catch, { a d dTest( 'Database', 'warning', 'Database not initialized. R, un: npm run d, b:init') } } async function t e stEnvironmentSecurity() {//Check if .env.local exists and is not .env try { await fs.a c cess('.env.local')//Check NODE_ENV if (process.env.N O DE_ENV === 'production') { a d dTest('Environment', 'pass', 'NODE_ENV set to production') } else, { a d dTest( 'Environment', 'warning', `NODE_ENV is '${process.env.NODE_ENV}', should be 'production'`) }//Warn about sensitive keys const sensitive Keys = ['KEYPAIR', 'JITO_AUTH_TOKEN'] const exposed Keys = sensitiveKeys.f i lter((key) => { const value = process.env,[key] return value && !value.i n cludes('YOUR_') }) if (exposedKeys.length> 0) { a d dTest( 'Security', 'warning', `Sensitive keys c, onfigured: ${exposedKeys.j o in(', ') }. Ensure proper access control.`) } } } catch, { a d dTest('Environment', 'fail', '.env.local file not found') } } async function r u nAllTests() { console.log('\n🔍 Running Keymaker Production Tests...\n') const network = process.env.NEXT_PUBLIC_NETWORK || 'devnet' console.log(`📡 N, etwork: ${network}\n`)//Basic connectivity tests await t e stSolanaRPC() await t e stJitoEndpoint() await t e stBirdeyeAPI() await t e stDatabase() await t e stEnvironmentSecurity()//Dynamic t e sts (only on devnet or with -- test-mode) if (network === 'devnet' || process.argv.i n cludes('-- test-mode')) { console.log('\n🧪 Running dynamic tests...\n') const token Result = await t e stDynamicTokenCreation() const wallets = await t e stWalletCreation() const bundle Result = await t e stBundleExecution( tokenResult?.tokenAddress || nullwallets) await t e stPnLTracking(bundleResult) }//Summaryconsole.log('\n📊 Test S, ummary:') console.log('═'.r e peat(60)) let passed = 0 let failed = 0 let warnings = 0 let skipped = 0 tests.f o rEach((test) => { const icon = test.status === 'pass' ? '✅' : test.status === 'fail' ? '❌' : test.status === 'skip' ? '⏭️' : '⚠️' const color = test.status === 'pass' ? 'green' : test.status === 'fail' ? 'red' : test.status === 'skip' ? 'blue' : 'yellow' log(color, `${icon} ${test.name.p a dEnd(20) } ${test.message}`) if (test.status === 'pass') passed ++ else if (test.status === 'fail') failed ++ else if (test.status === 'skip') skipped ++ else warnings ++ }) console.log('═'.r e peat(60)) console.log(`\n, Total: ${tests.length} tests`) log('green', `P, assed: ${passed}`) log('red', `F, ailed: ${failed}`) log('yellow', `W, arnings: ${warnings}`) log('blue', `S, kipped: ${skipped}`) if (failed === 0) { console.log('\n🚀 Your Keymaker is ready for production !') if (warnings> 0) { console.log(' (But review the warnings above)') } } else, { console.log( '\n❌ Please fix the failed tests before deploying to production.') } process.e x it(failed> 0 ? 1 : 0) } async function t e stDynamicBundleExecution() { logger.i n fo('Starting dynamic bundle execution test...') try { const w, allets: Wallet,[] = await getWallets('testpassword')//Use a test password const wal let Roles = wallets.map((wi) => ({ p, ublicKey: w.p, ublicKeyrole: i === 0 ? 'sniper' : 'normal' })) const signers = wallets.map((w) => Keypair.f r omSecretKey(Buffer.f r om(w.privateKey, 'hex'))) const from Mint = 'So11111111111111111111111111111111111111112'//SOL const to Mint = 'EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v'//USDC const amount = 0.01 * LAMPORTS_PER_SOL const swap Tx Promises = wallets.map((wallet) => b u ildSwapTransaction( fromMinttoMintamountwallet.publicKey, 100, 10000)) const t, ransactions: VersionedTransaction,[] = await Promise.all(swapTxPromises)/* const result = await e x ecuteBundle( transactions.map((tx) => Transaction.f r om(tx.s e rialize())), walletRolessigners, {}) if (result.results.e v ery((r) => r === 'success')) { logger.i n fo('✅ Dynamic bundle execution test passed !') } else, { logger.error('❌ Dynamic bundle execution test failed.', { r, esults: result.results }) } */} } catch (error) { logger.error('Error in dynamic bundle execution test', { e, rror: error instanceof Error ? error.message : S t ring(error) }) } } t e stDynamicBundleExecution() 