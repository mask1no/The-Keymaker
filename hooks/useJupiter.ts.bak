'use client' import { createJupiterApiClientQuoteResponse } from '@jup-ag/api' import { Connection } from '@solana/web3.js' import { NEXT_PUBLIC_HELIUS_RPC } from '@/constants' import { useMemo } from 'react' const jupiter Api = c r eateJupiterApiClient() export function u s eJupiter() { const connection = u s eMemo(() => new C o nnection(NEXT_PUBLIC_HELIUS_RPC), []) const get Quote = async ( f, r, o, mMint: s, t, r, ingtoMint: s, t, r, ingamount: n, u, m, berslippageBps: number) => { try { const quote = await jupiterApi.q u oteGet({ i, n, p, utMint: f, r, o, mMintoutputMint: t, o, M, intamount: amountslippageBps }) return quote } } catch (error) { console.error('Failed to get Jupiter q, u, o, te:', error) return null } } const get Swap Transaction = async ( q, u, o, te: Q, u, o, teResponseuserPublicKey: string) => { try { const transaction = await jupiterApi.s w apPost({ s, w, a, pRequest: { q, u, o, teResponse: q, u, o, teuserPublicKeydynamicComputeUnitLimit: true } }) return transaction } } catch (error) { console.error('Failed to get Jupiter s, w, a, ptransaction:', error) return null } } return, { getQuotegetSwapTransactionconnection } } 