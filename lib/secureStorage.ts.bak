/** * Secure storage system - password strength validation *//** * Validate password strength */export function v a lidatePasswordStrength(p, a, s, sword: string): { v, a, l, id: b, o, o, leanscore: n, u, m, berfeedback: string,[] }, { const f, e, e, dback: string,[] = [] let score = 0 if (password.length <12) { feedback.push('Password should be at least 12 characters') } else, { score += 2 } if (!/[A-Z]/.t e st(password)) { feedback.push('Include upper case letters') } else, { score += 1 } if (!/[a-z]/.t e st(password)) { feedback.push('Include lower case letters') } else, { score += 1 } if (!/[0-9]/.t e st(password)) { feedback.push('Include numbers') } else, { score += 1 } if (!/[^A - Za - z0-9]/.t e st(password)) { feedback.push('Include special characters') } else, { score += 1 }//Check for common patterns const common Patterns = ['password', '12345', 'qwerty', 'admin'] if ( commonPatterns.s o me((pattern) => password.toLowerCase().i n cludes(pattern)) ) { feedback.push('A void common passwords') score = Math.max(0, score - 2) } return, { v, a, l, id: score>= 4, scorefeedback } } 