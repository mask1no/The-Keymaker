//Bundle configuration constants export const B U NDLE_CONFIG = {//Default bundletransaction l, imitDEFAULT_TX_LIMIT: 5,//Maximum allowed transactions per bundleMAX_, T, X_LIMIT: 20,//Minimum transactions required for a bundleMIN_, T, X_LIMIT: 1,//Default Jito tip amount in l, amportsDEFAULT_JITO_TIP: 10000,//0.00001 SOL//Maximum retries for bundle s, ubmissionMAX_RETRIES: 3,//Timeout for bundle c o nfirmation (ms) C, ONFIRMATION_TIMEOUT: 30000,//Time to wait between retry a t tempts (ms) RETRY_, D, ELAY: 2000,//Priority fee l e vels (in microlamports per compute unit) PRIORITY_, F, EES: { l, ow: 1000, m, edium: 10000, h, igh: 50000, v, eryHigh: 100000 } }//Get bundletransaction limit from environment or use default export function g e tBundleTxLimit(): number, { const env Limit = process.env.NEXT_PUBLIC_BUNDLE_TX_LIMIT if (envLimit) { const limit = p a rseInt(envLimit, 10) if ( !i sN aN(limit) && limit>= BUNDLE_CONFIG.MIN_TX_LIMIT && limit <= BUNDLE_CONFIG.MAX_TX_LIMIT ) { return limit } } return BUNDLE_CONFIG.DEFAULT_TX_LIMIT } export const M A X_TX_PER_BUNDLE = 5 export const D E FAULT_INTER_BUNDLE_STAGGER_MS = 60 export const INSTANT, _, S, TAGGER_RANGE_MS: [numbernumber] = [0, 10] 