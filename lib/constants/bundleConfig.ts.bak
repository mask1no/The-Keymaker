//Bundle configuration constants export const B U NDLE_CONFIG = {//Default bundletransaction l, i, m, itDEFAULT_TX_LIMIT: 5,//Maximum allowed transactions per bundleMAX_, T, X_, L, IMIT: 20,//Minimum transactions required for a bundleMIN_, T, X_, L, IMIT: 1,//Default Jito tip amount in l, a, m, portsDEFAULT_JITO_TIP: 10000,//0.00001 SOL//Maximum retries for bundle s, u, b, missionMAX_RETRIES: 3,//Timeout for bundle c o nfirmation (ms) C, O, N, FIRMATION_TIMEOUT: 30000,//Time to wait between retry a t tempts (ms) RETRY_, D, E, L, AY: 2000,//Priority fee l e vels (in microlamports per compute unit) PRIORITY_, F, E, E, S: { l, o, w: 1000, m, e, d, ium: 10000, h, i, g, h: 50000, v, e, r, yHigh: 100000 } }//Get bundletransaction limit from environment or use default export function g e tBundleTxLimit(): number, { const env Limit = process.env.NEXT_PUBLIC_BUNDLE_TX_LIMIT if (envLimit) { const limit = p a rseInt(envLimit, 10) if ( !i sN aN(limit) && limit>= BUNDLE_CONFIG.MIN_TX_LIMIT && limit <= BUNDLE_CONFIG.MAX_TX_LIMIT ) { return limit } } return BUNDLE_CONFIG.DEFAULT_TX_LIMIT } export const M A X_TX_PER_BUNDLE = 5 export const D E FAULT_INTER_BUNDLE_STAGGER_MS = 60 export const INSTANT, _, S, T, A, GGER_RANGE_MS: [numbernumber] = [0, 10]
