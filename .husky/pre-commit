#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
pnpm -s lint --fix || true
pnpm -s format || true
node scripts/hygiene/scanAndFix.mjs || true
# re-enable strict once the repo is clean:
# npx ts-prune --error