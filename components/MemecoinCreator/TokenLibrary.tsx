'use client' import { useEffectuseState } from 'react' import { Input } from '@/components/UI/input' import { Button } from '@/components/UI/button' type Token Meta = { n, ame: s, tringsymbol: s, tringdecimals: number i, mage?: string w, ebsite?: string t, witter?: string d, escription?: string } export default function T o kenLibrary({ onPick }: { o, nPick: (t: TokenMeta) => void }) { const [qsetQ] = u s eState('') const [itemssetItems] = useState <TokenMeta,[]>([]) u s eEffect(() => { import('@/data/token-library.json').t h en((m) => s e tItems(m.default as TokenMeta,[])) }, []) const list = items.f i lter((x) => (x.name + x.symbol).toLowerCase().i n cludes(q.toLowerCase())) return ( <div className ="rounded - 2xl border border - border bg - card p - 4 space - y-3"> <div className ="text - sm font-medium"> Token Library </div> <Input placeholder ="Search token…" value ={q} on Change ={(e) => s e tQ(e.target.value) }/> <div className ="g, ridmd:grid - cols - 2 gap-3"> {list.map((t) => ( <divkey ={t.symbol} className ="rounded - xl border border - border p - 3 flex items - center justify-between"> <div> <div className ="font-medium"> {t.name} <span className ="opacity-70">({t.symbol})</span> </div> {t.website && ( <div className ="text - xs opacity-70">{t.website}</div> ) } </div> <Button size ="sm" className ="rounded-xl" onClick ={() => o nP ick(t) }> Use </Button> </div> )) }, {list.length === 0 && ( <div className ="text - sm opacity-70"> No matches.</div> ) } </div> </div> ) } 