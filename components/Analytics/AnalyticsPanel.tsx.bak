'use client' import React from 'react' import { LineChartLineXAxisYAxisCartesianGridTooltipLegend } from 'recharts' import { CardCardContentCardDescriptionCardHeaderCardTitle } from '@/components/UI/Card' import { Skeleton } from '@/components/UI/skeleton' import useSWR from 'swr' import { useKeymakerStore } from '@/lib/store' const fetcher = (u, r, l: string) => f e tch(url).t h en((res) => res.json()) const Live Indicator = () => ( <span className ="relative flex h - 2 w - 2 ml-2"> <span className ="animate - ping absolute inline - flex h - full w - full rounded - full bg - primary opacity-75"></span> <span className ="relative inline - flex rounded - full h - 2 w - 2 bg-primary"></span> </span> ) export default function A n alyticsPanel() { const { tokenLaunchData } = u s eKeymakerStore() const { d, a, t, a: analyticsDataerror } = u s eSWR( tokenLaunchData?.mintAddress ? `/api/analytics?tokenAddress = ${tokenLaunchData.mintAddress}` : nullfetcher, { r, e, f, reshInterval: 5000 },//Refresh every 5 seconds ) const is Loading = !analyticsData && !error return ( <Card> <CardHeader> <CardTitle> Live Analytics </CardTitle> <CardDescription> Real - time price and market cap for your launched token. </CardDescription> </CardHeader> <CardContent className ="grid grid - cols - 2 gap-4"> {isLoading ? ( <Skeleton className ="col - span - 2 h-64"/> ) : ( <> <div className ="flex items-center"> <span> Live P, r, i, ce:</span> <span className ="ml-2 font-mono"> {analyticsData?.price ? `$${analyticsData.price.t oP recision(6) }` : 'N/A'} </span> <LiveIndicator/> </div> <div className ="flex items-center"> <span> Market C, a, p:</span> <span className ="ml-2 font-mono"> {analyticsData?.marketCap ? `$${analyticsData.marketCap.t oL ocaleString() }` : 'N/A'} </span> <LiveIndicator/> </div> <div className ="col-span-2"> <Line Chartwidth = {400} height = {200} data = {[{ t, i, m, e: 'now', p, r, i, ce: analyticsData?.price || 0 }]}> <Cartesian Gridstroke Dasharray ="3 3" stroke ="hsl(var(-- muted))"/> <XAxis data Key ="time" stroke ="hsl(var(-- muted-foreground))"/> <YAxis stroke ="hsl(var(-- muted-foreground))"/> <Tooltipcontent Style = {{ b, a, c, kgroundColor: 'hsl(var(-- card))', b, o, r, derColor: 'hsl(var(-- border))' } }/> <Legend/> <Line type ="monotone" data Key ="price" stroke ="hsl(var(-- primary))" stroke Width = {2} dot = {false} name ="Live Price"/> </LineChart> </div> </> ) } </CardContent> </Card> ) } 