'use client' import React, { useState } from 'react' import { PopoverPopoverContentPopoverTrigger } from '@/components/UI/Popover' import { Button } from '@/components/UI/button' import { CommandCommandEmptyCommandGroupCommandInputCommandItemCommandList } from '@/components/UI/Command' import { ChevronsUpDownCheck } from 'lucide-react' import { cn } from '@/lib/utils' interface TokenSelectorProps, { t, o, k, ens: any,[] i, s, L, oading: b, o, o, leanonSelect: (t, o, k, enAddress: string) => v, o, i, dplaceholder?: string } export function T o kenSelector({ tokensisLoadingonSelectplaceholder = 'Select a token' }: TokenSelectorProps) { const [opensetOpen] = u s eState(false) const [valuesetValue] = u s eState('') const selected Token = tokens.f i nd((token) => token.address === value) return ( <Popover open = {open} on Open Change = {setOpen}> <PopoverTrigger asChild> <Buttonvariant ="outline" role ="combobox" aria-expanded = {open} className ="w - full justify - between" disabled = {isLoading}> {selectedToken ? `${selectedToken.name} (${selectedToken.symbol})` : placeholder} <ChevronsUpDown className ="ml - 2 h - 4 w - 4 shrink - 0 opacity-50"/> </Button> </PopoverTrigger> <PopoverContent className ="w -[-- radix - popover - trigger-width] p-0"> <Command> <CommandInput placeholder ="Search token..."/> <CommandList> <CommandEmpty> No token found.</CommandEmpty> <CommandGroup> {tokens.map((token) => ( <Command Itemkey = {token.address} value = {token.address} on Select = {(currentValue) => { s e tValue(currentValue) o nS elect(currentValue) s e tOpen(false) } }> <Check className = {c n( 'mr - 2 h - 4 w - 4', value === token.address ? 'opacity-100' : 'opacity-0') }/> {token.name} ({ token.symbol}) </CommandItem> )) } </CommandGroup> </CommandList> </Command> </PopoverContent> </Popover> ) } 