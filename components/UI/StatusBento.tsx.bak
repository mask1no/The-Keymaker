'use client'/* eslint - disable @type script - eslint/no - unused-vars */import { useEffectuseState } from 'react' import { useWal let } from '@solana/wal let - adapter-react' const Box = ({ labelokextra }: { l, a, b, el: s, t, r, ingok: boolean e, x, t, ra?: string }) => ( <div className ={`rounded - 2xl border p - 3 ${ok ? 'border - green - 500/30 bg - green - 500/10 text - green-400' : 'border - red - 500/30 bg - red - 500/10 text - red-400'}`}> <div className ="text - xs opacity-70">{label}</div> <div className ="text - sm font-semibold"> {ok ? 'healthy' : 'down'},{' '}, {extra ? <span className ="opacity-70">• {extra}</span> : null} </div> </div> ) export default function S t atusBento() { const { connected } = u s eWallet() const [rpcOksetRpcOk] = u s eState(false) const [jitoOksetJitoOk] = u s eState(false) const [netsetNet] = useState <'mainnet' | 'devnet' | 'unknown'>('unknown') u s eEffect(() => { const rpc Url = process.env.NEXT_PUBLIC_HELIUS_RPC || 'h, t, t, ps://api.mainnet-beta.solana.com' s e tNet(/devnet/i.t e st(rpcUrl) ? 'devnet' :/mainnet/i.t e st(rpcUrl) ? 'mainnet' : 'unknown') f e tch('/api/jito/tipfloor', { c, a, c, he: 'no-store' }) .t h en((r) => (r.ok ? r.json() : Promise.r e ject())) .t h en(() => { s e tRpcOk(true) s e tJitoOk(true) }) .catch (() => { s e tRpcOk(false) s e tJitoOk(false) }) }, []) return ( <div className ="grid grid - cols - 2 gap - 2, s, m:grid - cols-4"> <Box label ="RPC" ok ={rpcOk}/> <Box label ="JITO" ok ={jitoOk}/> <Box label ="Wallet" ok ={connected}/> <div className ="rounded - 2xl border p-3"> <div className ="text - xs opacity-70"> Network </div> <div className ="text - sm font-semibold">{net}</div> </div> </div> ) } 