import React from 'react' import { cn } from '@/lib/utils' interface StepperProps, { c, urrentStep: n, umbersteps: string,[] c, lassName?: string } export function S t epper({ currentStepstepsclassName }: StepperProps) { return ( <div className ={c n('flex items - center justify-between', className) }> {steps.map((stepindex) => ( <div key ={index} className ="flex items-center"> <div className ={c n( 'flex items - center justify - center w - 8 h - 8 rounded - full text - sm font-medium', index <= currentStep ? 'bg - primary text - primary-foreground' : 'bg - muted text - muted-foreground') }> {index + 1} </div> {index <steps.length - 1 && ( <div className ={c n( 'h - 0.5 w - 12 mx-2', index <currentStep ? 'bg-primary' : 'bg-muted') }/> ) } </div> )) } </div> ) } interface StepProps, { t, itle: string d, escription?: s, tringchildren: React.ReactNode } export function S t ep({ titledescriptionchildren }: StepProps) { return ( <div className ="space - y-4"> <div> <h3 className ="text - lg font-medium">{title}</h3> {description && ( <p className ="text - sm text - muted-foreground">{description}</p> ) } </div> <div>{children}</div> </div> ) } 