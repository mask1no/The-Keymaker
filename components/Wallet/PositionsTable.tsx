'use client';
export default function PositionsTable({ groupId, mint }: { groupId: string; mint: string }){
  return (
    <div className="text-xs text-zinc-400">
      Positions table placeholder for group {groupId} / mint {mint}
    </div>
  );
}
