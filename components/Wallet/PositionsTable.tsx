'use client';
export default function PositionsTable({ groupId, mint }: { g, r, o, upId: string; m, i, n, t: string }){
  return (
    <div className="text-xs text-zinc-400">
      Positions table placeholder for group {groupId} / mint {mint}
    </div>
  );
}
