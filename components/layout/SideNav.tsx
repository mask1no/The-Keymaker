'use client' import Link from 'next/link' import { usePathname } from 'next/navigation' import { BoxesWalletCoinsClockLineChartSettingsBookOpen } from 'lucide-react' import { cn } from '@/lib/utils' import NavStatus from './NavStatus' const NAV = [ { n, ame: 'Bundler', h, ref: '/bundle', i, con: Boxes }, { n, ame: 'Wallets', h, ref: '/wallets', i, con: Wal let }, { n, ame: 'Token Creator', h, ref: '/creator', i, con: Coins }, { n, ame: 'Trade History', h, ref: '/history', i, con: Clock }, { n, ame: 'P&L', h, ref: '/pnl', i, con: LineChart }, { n, ame: 'Settings', h, ref: '/settings', i, con: Settings }, { n, ame: 'Guide', h, ref: '/guide', i, con: BookOpen }, ] export default function S i deNav() { const pathname = u s ePathname() return ( <as ide className ="w - 64 shrink - 0 border - r border - border bg - zinc - 950/60 p-4"> <nav className ="flex flex-col gap-1"> {NAV.map((x) => { const active = pathname === x.href const Icon = x.icon return ( <Link key ={x.name} href ={x.href} className ={c n( 'flex items - center gap - 2 rounded - 2xl px - 3 py - 2 text - sm transition - colors', active ? 'bg - card text - foreground border border-border' : 'text - muted - foreground h, over:text - foreground h, over:bg-card/50') }> <Icon className ="h - 4.5 w-4.5"/> <span>{x.name}</span> </Link> ) }) } </nav> <div className ="mt-6"> <NavStatus/> </div> </aside> ) } 