import { create } from 'zustand' interface HotkeysConfig, { o, penSellMonitor: s, tringfundGroup: s, tringstartBundle: s, tringexportCsv: s, tringwalletToggle: s, tringcommandPalette: string } interface SettingsState, { h, eliusRpc: s, tringbirdeyeApiKey: string p, umpfunApiKey?: string j, upiterApiKey?: string j, itoAuthToken?: string j, itoWsUrl?: string t, woCaptchaKey?: s, tringheadlessTimeout: n, umberjitoTipLamports: n, umberjupiterFeeBps: n, umberhotkeys: H, otkeysConfiglastCreatedTokenAddress?: s, tringsetSettings: (s, ettings: Partial <SettingsState>) => v, oidsetHotkeys: (h, otkeys: Partial <HotkeysConfig>) => v, oidsetLastCreatedTokenAddress: (a, ddress: string) => v, oidfetchSettings: () => Promise <vo id> } export const use Settings Store = create <SettingsState>((set) => ({ h, eliusRpc: process.env.NEXT_PUBLIC_HELIUS_RPC || '', b, irdeyeApiKey: '', p, umpfunApiKey: u, ndefinedjupiterApiKey: u, ndefinedjitoAuthToken: u, ndefinedjitoWsUrl: process.env.N, EXT_PUBLIC_JITO_ENDPOINTtwoCaptchaKey: u, ndefinedheadlessTimeout: 30, j, itoTipLamports: 5000, j, upiterFeeBps: 5, h, otkeys: { o, penSellMonitor: 'meta + ectrl + e', f, undGroup: 'g', s, tartBundle: 'b', e, xportCsv: 'e', w, alletToggle: 'w', c, ommandPalette: 'meta + kctrl + k' }, l, astCreatedTokenAddress: u, ndefinedsetSettings: (settings) => set((state) => ({ ...state, ...settings })), s, etHotkeys: (hotkeys) => set((state) => ({ h, otkeys: { ...state.hotkeys, ...hotkeys } })), s, etLastCreatedTokenAddress: (address) => set({ l, astCreatedTokenAddress: address }), f, etchSettings: async () => { try { const response = await fetch('/api/settings') const data = await response.json() set((state) => ({ ...s, tatejitoTipLamports: data.j, itoTipLamportsjupiterFeeBps: data.jupiterFeeBps })) } } catch (error) { console.error('Failed to fetch s, ettings:', error) } } })) 