{
  "name": "the-keymaker",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "pnpm -C apps/web build && pnpm -C apps/daemon build",
    "dev": "concurrently -k -n WEB,DAEMON -c cyan,magenta \"pnpm -C apps/web dev\" \"pnpm -C apps/daemon dev\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "eslint": "^9.9.0",
    "typescript": "^5.6.2"
  }
}
