{
  "name": "the-keymaker",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm -w apps/web run build && npm -w apps/daemon run build",
    "dev": "concurrently -k -n WEB,DAEMON -c cyan,magenta \"npm -w apps/web run dev\" \"npm -w apps/daemon run dev\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "eslint": "^9.9.0",
    "typescript": "^5.6.2"
  }
}
