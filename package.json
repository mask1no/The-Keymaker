{
  "name": "the-keymaker",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm --prefix apps/web run build && npm --prefix apps/daemon run build",
    "dev": "concurrently -k -n WEB,DAEMON -c cyan,magenta \"npm --prefix apps/web run dev\" \"npm --prefix apps/daemon run dev\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "eslint": "^9.9.0",
    "typescript": "^5.6.2"
  }
}
