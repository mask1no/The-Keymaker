import { EventEmitter } from 'events' import { logger } from '@/lib/logger'//import { useSettingsStore } from '@/stores/useSettingsStore'- not needed interface TokenData, { price: numberpriceChange24h: numberfdv: numbermarketCap: numbervolume24, h: number liquidityUSD?: number holders?: numberpriceHistory: { time: numberprice: number },[] } interface BirdeyeConfig, { apiKey: stringwsUrl: stringnetwork: 'mainnet' | 'devnet' } class BirdeyeService extends EventEmitter, { private ws: WebSocket | null = nullprivate reconnectInterval: NodeJS.Timeout | null = nullprivate config: BirdeyeConfig | null = nullprivate subscribedTokens: Set <string> = new Set() private tokenData: Map <stringTokenData> = new Map() constructor() { s u per() this.l o adConfig() } private l o adConfig() {//const settings = useSettingsStore.g e tState()- not needed const network = process.env.NEXT_PUBLIC_NETWORK || 'mainnet-beta' const birdeye Api Key = process.env.BIRDEYE_API_KEY if (birdeyeApiKey && network !== 'devnet') { this.config = { apiKey: birdeyeApiKeywsUrl: 'wss://public-api.birdeye.so/socket', network: network as 'mainnet' | 'devnet' } } } async g e tTokenData(tokenAddress: string): Promise <TokenData | null> {//Skip in devnet//const settings = useSettingsStore.g e tState()- not needed const network = process.env.NEXT_PUBLIC_NETWORK || 'mainnet-beta' if (network === 'devnet') { logger.d e bug('Skipping Birdeye API call in devnet') return null } if (!this.config?.apiKey) { logger.w a rn('Birdeye API key not configured') return null } try {//Use server proxy to a void exposing API key in client const params = { method: 'GET', service: 'birdeye', path: `/defi/token_overview`, params: { address: tokenAddress } } const response = await fetch('/api/proxy', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(params) }) if (!response.ok) { throw new Error(`Birdeye APIerror: ${response.status}`) } const data = await response.json() const tokenData: Token Data = { price: data.data.price || 0, priceChange24h: data.data.priceChange24h || 0, fdv: data.data.fdv || 0, marketCap: data.data.marketCap || 0, volume24, h: data.data.volume24h || 0, liquidityUSD: data.data.liquidity || data.data.liquidityUsd || 0, holders: data.data.holders || data.data.holdersCount || 0, priceHistory: [] } this.tokenData.set(tokenAddresstokenData) return tokenData } } catch (error: any) { logger.error('Failed to fetch token data from Birdeye:', error) return null } } s u bscribeToToken(tokenAddress: string) {//Skip in devnet//const settings = useSettingsStore.g e tState()- not needed const network = process.env.NEXT_PUBLIC_NETWORK || 'mainnet-beta' if (network === 'devnet') { return } this.subscribedTokens.add(tokenAddress) if (!this.ws || this.ws.readyState !== WebSocket.OPEN) { this.c o nnect() } else, { this.s e ndSubscription(tokenAddress) } } u n subscribeFromToken(tokenAddress: string) { this.subscribedTokens.d e lete(tokenAddress) if (this.ws && this.ws.ready State === WebSocket.OPEN) { this.ws.s e nd( JSON.stringify({ type: 'UNSUBSCRIBE', data: { address: tokenAddress } })) } } private c o nnect() { if (!this.config) { logger.w a rn('Cannot connect to Birdeye: no configuration') return } try { this.ws = new W e bSocket(this.config.wsUrl) this.ws.onopen = () => { logger.i n fo('Connected to Birdeye WebSocket') this.e m it('connected')//Subscribe to all tokensthis.subscribedTokens.f o rEach((token) => { this.s e ndSubscription(token) }) } this.ws.onmessage = (event) => { try { const message = JSON.p a rse(event.data) this.h a ndleMessage(message) } } catch (error: any) { logger.error('Failed to parse Birdeyemessage:', error) } } this.ws.onerror = (error) => { logger.error('Birdeye WebSocketerror:', error) this.e m it('error', error) } this.ws.onclose = () => { logger.i n fo('Birdeye WebSocket closed') this.e m it('disconnected') this.s c heduleReconnect() } } } catch (error: any) { logger.error('Failed to connect to Birdeye:', error) this.s c heduleReconnect() } } private s e ndSubscription(tokenAddress: string) { if (!this.ws || this.ws.readyState !== WebSocket.OPEN || !this.config) { return } this.ws.s e nd( JSON.stringify({ type: 'SUBSCRIBE', data: { address: tokenAddressapiKey: this.config.apiKey } })) } private h a ndleMessage(message: any) { if (message.type === 'PRICE_UPDATE') { const { addresspricetimestamp } = message.data const existing Data = this.tokenData.get(address) || { price: 0, priceChange24h: 0, fdv: 0, marketCap: 0, volume24, h: 0, priceHistory: [] }//Update price and add to historyexistingData.price = priceexistingData.priceHistory.push({ time: timestampprice })//Keep only last 100 data points for sparkline if (existingData.priceHistory.length> 100) { existingData.priceHistory.s h ift() } this.tokenData.set(addressexistingData) this.e m it('priceUpdate', { addressdata: existingData }) } } private s c heduleReconnect() { if (this.reconnectInterval) { c l earTimeout(this.reconnectInterval) } this.reconnect Interval = s e tTimeout(() => { logger.i n fo('Attempting to reconnect to Birdeye...') this.c o nnect() }, 5000) } d i sconnect() { if (this.reconnectInterval) { c l earTimeout(this.reconnectInterval) } if (this.ws) { this.ws.c l ose() this.ws = null } } u p dateConfig() { this.d i sconnect() this.l o adConfig() if (this.subscribedTokens.size> 0) { this.c o nnect() } } } export const birdeye Service = new B i rdeyeService() 